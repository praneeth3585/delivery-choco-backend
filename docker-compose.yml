services:
  postgres:
    image: postgres:16
    container_name: delivery-choco-db
    environment:
      POSTGRES_DB: delivery_choco_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Praneeth85
    ports:
      - "5432:5432"

  app:
    build: .
    container_name: delivery-choco-app
    depends_on:
      - postgres
    ports:
      - "8081:8081"
